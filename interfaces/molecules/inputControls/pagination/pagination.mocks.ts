import { IPagination } from './pagination';

const Pagination: IPagination = {
  text: 'Pagination',
  type: 'solid',
  onClickHandler: ()=>{}
};

export const mockPaginationProps = {
  Pagination,
};